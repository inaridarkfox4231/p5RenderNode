#version 300 es
precision mediump float;

in vec2 aPosition;
out vec2 vUv;

void main(void){
  vUv = aPosition * 0.5 + 0.5;
  vUv.y = 1.0 - vUv.y;
  gl_Position = vec4(aPosition, 0.0, 1.0);
}
